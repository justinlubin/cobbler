(((dot (x y) 
         ((Assign out (Num 0))
         (For i (Call range (Call len x)) 
                ((Assign out (Call + out (Call *
   (Index x i) (Index y i))))
          ))
	 (Return out)))
   (sum (x) 
           ((Assign out (Num 0))
           (For i (Call range (Call len x))
                  ((Assign out (Call + out (Index x i)))))
	   (Return out)))
        (mul (x y)
           ((Assign out (Call zeros (Call len x)))
            (For i (Call range (Call len x))
               ((Assign (Index out i) (Call * (Index x i) (Index y i)))))
	    (Return out)
           ))) () )
